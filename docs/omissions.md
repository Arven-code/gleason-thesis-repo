# What is not fully reconstructed here

This repository is as complete as possible from the code that was visible in the working conversation.

## Not included as final runnable code

- Some intermediate exploratory scripts that existed in near-duplicate versions.
- One ablation runner that was only available in truncated form in the working notes.
- Machine-specific paths remain in several archived scripts, because preserving the original working form was more useful than silently rewriting them.

## Recommended next step

After cloning or uploading this repository, replace hard-coded paths with config-driven ones and promote any still-needed archived script into `src/`.
