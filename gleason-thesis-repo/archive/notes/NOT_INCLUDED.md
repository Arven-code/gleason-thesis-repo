Scripts mentioned in chat but not included as runnable files here:

- `run_ablation_cls.py` — pasted only partially, so it was incomplete
- several older duplicate variants of already-kept scripts
- some exploratory snippets that were pure fragments rather than full scripts
